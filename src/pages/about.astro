---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const pageTitle = 'Sobre mi';

const identity = {
    firsName: "Sarah",
    country: "Canada",
    occupation: "Escritor técnico",
    hobbies: ["Fotografia", "observacion de aves", "peñarol"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Redaccion de documentos"]

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy"
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{pageTitle}</title>
    <style define:vars={{skillColor}}>
        h1 {
            color: purple;
            font-size: 4rem;
        }
        .skill {
            color: var(--skillColor);
            font-weight: bold;
        }
    </style>
</head>
<body>
    <Header />

    <p>Aca hay algunos datos sobre mi</p>
    <ul>
        <li>Me llamo {identity.firsName}</li>
        <li>Vivo en {identity.country} y trabajo como {identity.occupation}. </li>
        {identity.hobbies.length >= 2 && 
            <li>Dos de mis aficiones son: {identity.hobbies[0]} y {identity.hobbies[2]}</li>
        }
    </ul>
    <h1>About</h1>
    <h1>{pageTitle}</h1>
    <h2>Mostrando mi sitio web</h2>
    <ul>
        {skills.map((skill) => <li>{skill}</li>)}
    </ul>

    <p>Mis habilidades son:</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul> 

    {happy && <p>¡Estoy feliz!</p>}
    {finished && <p>¡He terminado el tutorial!</p> }
    {goal == 3 ? <p>Mi Objetivo es terminar en 3 dias</p> : <p>Mi objetivo no son 3 dias</p>}



    <Footer/>


    
</body>
</html>